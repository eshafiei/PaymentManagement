/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ActiveAccountsFilterPipe } from './active-accounts-filter.pipe';

describe('Pipe: ActiveAccounte', () => {
  it('create an instance', () => {
    let pipe = new ActiveAccountsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
